{% extends 'base.html.twig' %}

{% block title %} Szkoły tańca {% endblock %}

{% block body %}
{#<div class="row">#}
{#    {% for szkola in szkoly %}#}
{#    <div class="col-6 col-md-3">#}
{#        <select class="form-control selectpicker show-tick" id="select-country" data-live-search="true">#}
{#            <option data-tokens="city">{{ szkola.miastoSzkoly.nazwaMiasta}} </option>#}

{#        </select>#}
{#    </div>#}
{#        <div class="col-6 col-md-3">#}
{#            <select class="form-control selectpicker show-tick" id="select-kategory" data-live-search="true">#}
{#                <option data-tokens="china">opcja </option>#}

{#            </select>#}
{#        </div>#}
{#    {% endfor %}#}
    <div>
        <label for="listSearch"></label><input class="form-control" id="listSearch" type="text" placeholder="🔍 Szukaj po wszystkim">
    </div>
<br>

    <div class="list-group">

        {% for szkola in szkoly %}
            <a href="{{ path('szkola_info', {"id":szkola.id}) }}" class="list-group-item list-group-item-action flex-column align-items-start" id="myList">
                <div class="d-flex w-100 justify-content-between">
                    <h5 class="mb-1" >{{ szkola.nazwaSzkoly }}</h5>
                                    <small class="text-muted">{{ szkola.miastoSzkoly.nazwaMiasta }}</small>
                </div>
                <p class="mb-1">{{ szkola.opisKrotki }}</p>
                {% for umiejetnosci in szkola.kategorie %}
                <small class="text-muted"><i class="far fa-check-circle"></i>{{ umiejetnosci.nazwaUmiejetnosci }}</small>
                {% endfor %}
            </a>
            <br>
        {% endfor %}
    </div>

{% endblock %}

{% block javascripts %}
<script>
    $(document).ready(function(){
        $("#listSearch").on("keyup", function() {
            var value = $(this).val().toLowerCase();
            $("#myList").filter(function() {
                $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
            });
        });
    });
</script>

{% endblock %}





